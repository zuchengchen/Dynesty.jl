# Notebook And Demo Coverage

The complete Python notebook to Julia `.jl` example mapping is maintained in
`docs/src/notebook_coverage.md` and rendered by Documenter. All new Julia demo
coverage scripts are included in the default `test/test_examples.jl` smoke
suite.
